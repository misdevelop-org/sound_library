name: sound_library
description: Easy UI sounds to directly use in your app through mis develop sound library database
version: 0.1.1
homepage: https://github.com/misdevelop-org/sound_library

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
